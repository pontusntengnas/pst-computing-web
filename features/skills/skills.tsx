import * as React from "react";
import { Header } from "../core-ui/text/header";

interface Props {}

export const Skills: React.VFC<Props> = () => {
  return (
    <>
      <Header text={"Things I do"} size={2} />
    </>
  );
};
